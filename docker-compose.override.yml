# Local override: default 5433 collides with another project's Postgres on this
# machine. The !override tag REPLACES the base ports list instead of appending.
services:
  postgres:
    ports: !override
      - "5434:5432"
